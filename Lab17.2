#include <stdio.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "Rus");

    int n;
    int c = 0;
    int k = 0;

    printf("N = ");
    scanf("%d", &n);
    
    if (n > 0) {
        int a[n];
        for (int i = 0; i < n; i++) {
            printf("a[%d] = ", i);
            scanf("%d", &a[i]);
	    }
    	int d = a[1] - a[0];

        for (int i = 2; i < n; i++) {
            if (d != a[i] - a[i - 1]) {
                k = 1;
            }
        }
        if (k == 0) {
            printf("%d", d);
        }
        else printf("0");
    }
    else printf("Необходимо ввести N больше 0");
    
    return 0;
}
