#include <stdio.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "Rus");

    int n, s;
    int c = 0;

    printf("N = ");
    scanf("%d", &n);
    
    if (n > 1) {
        int a[n];
        for (int i = 0; i < n; i++) {
            printf("a[%d] = ", i + 1);
            scanf("%d", &a[i]);
	    }
	    if (a[0] > a[1]) {
            s = a[0];
            c = 1;
	    }
        for (int i = 1; i < n - 1; i++) {
            if ((a[i] > a[i - 1]) && (a[i] > a[i + 1])) {
                c = i + 1;
            }
        }
        if (a[n - 1] > a[n - 2]) {
            c = n;
	    }
        printf("Номер последнего локального максимума: %d", c);
    }
    else printf("Необходимо ввести N больше 0");
    
    return 0;
}
