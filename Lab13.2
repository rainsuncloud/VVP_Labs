#include <stdio.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "Rus");

    int a;
    float k = 1.1;
    float p = 1;

    printf("N = ");
    scanf("%d", &a);
    
    for (a; a > 0; a--) {
        p = p * k;
        k = k + 0.1;
    }

    printf("N = %f", p);

    return 0;
}
